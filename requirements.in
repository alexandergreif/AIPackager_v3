#
# This file is used to specify the dependencies of the project.
#
# To update the requirements.txt file, run:
#
#   pip-compile requirements.in
#
# To install the dependencies, run:
#
#   pip install -r requirements.txt
#
# Note: msitools is a system dependency and must be installed separately.
# See docs/deployment.md for installation instructions.
#
flask
python-dotenv
flask-socketio

# MCP Integration
mcp>=1.0.0
anyio>=4.0.0
